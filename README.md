# STRATOQUANT_NEXUS_PROJECT

This repo bootstraps the StratoQuant L0–L100 engine.

Quickstart
---------
1. Create virtualenv:
   python -m venv .venv
   source .venv/bin/activate
2. Install:
   pip install -r requirements.txt
3. Run local webhook:
   python src/app.py

Repo layout: core/, src/, pine/, config/, docs/, tests/, .devcontainer/, .github/
